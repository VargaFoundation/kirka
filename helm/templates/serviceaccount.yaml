{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kirka.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kirka.labels" . | nindent 4 }}
{{- end }}
