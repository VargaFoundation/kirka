apiVersion: v1
kind: Service
metadata:
  name: {{ include "kirka.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kirka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kirka.selectorLabels" . | nindent 4 }}
